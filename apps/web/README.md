# Studio web boundary

The active Studio is delivered through OpenAI Sites. This directory is reserved for shared client adapters or a future synchronized source boundary; it is not a second competing web application.

The Studio consumes versioned JSON Schemas from `packages/contracts/schema` and delegates native media work to independently deployable workers.
