# Worker-control boundary

This future lightweight service translates durable Studio jobs into Ampersand-owned worker contracts. It must keep provider IDs secondary, issue short-lived object access, enforce workspace scope, and never execute native media processing itself.
