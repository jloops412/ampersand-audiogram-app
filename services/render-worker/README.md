# Render-worker boundary

Authoritative audiogram and caption rendering will live here under issue #27. It will consume an immutable shared render specification and precomputed media features, then render deterministically with admitted native tools. Browser real-time capture is not a production renderer.
