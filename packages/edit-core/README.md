# Edit-core boundary

Future deterministic edit decisions use integer microseconds, half-open intervals, immutable commands, and replayable time maps. No browser-only state becomes the authoritative edit model.
