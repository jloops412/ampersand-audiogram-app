# Render-spec boundary

Future Studio preview and server export will share one immutable, versioned visual specification for dimensions, timing, layers, typography, captions, waveform data, and brand assets.
